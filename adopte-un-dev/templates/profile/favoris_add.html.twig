{% extends 'base.html.twig' %}

{% block title %}Mon Profil
{% endblock %}

{% block body %}

    
<div id="list-favorites" class="list-group">
    {% for favori in favorites %}

  <a class="dropdown-item" href="{{ path('profile_favoris_add', {'id': favori.id}) }}" onclick="window.location.href=this.href;">{{favori.username}}</a>
  {% endfor %}

{% endblock %}